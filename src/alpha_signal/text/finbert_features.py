"""Placeholder entry points for future FinBERT embedding generation and Pinecone features."""


def generate_finbert_embeddings():
    raise NotImplementedError(
        "FinBERT embedding generation will be added after the tabular baselines are stable."
    )


def build_similarity_features():
    raise NotImplementedError(
        "Nearest-neighbor event similarity features will be added in a later milestone."
    )
